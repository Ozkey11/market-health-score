# Market Health Score — 変更点 (v11.0 / 2026-07-25)

**需給データのスコア組み込み・チャート大幅拡張・お気に入り一覧の情報量増加・同期不具合の修正**

v10.9 からの差分です。今回は `index.html` のみの変更で、`batch/` 配下と
ワークフローは v10.9 のままです。

メジャー番号を上げたのは、スコアの算出に需給データが加わり、
チャートの構成が大きく変わったためです。

---

## A. 不具合の修正 — お気に入りとスコア分析の同期

### 症状

> スコア分析のページで価格をスキャンし、お気に入りに登録した後、
> お気に入りページに遷移すると、スコアが反映されていない

### 原因

2か所に問題がありました。

**① ☆ボタンがスコアを保存していなかった**

`Favorites.toggle()` は `{コード, 名前, 追加日時}` だけを保存する実装でした。
スコアを書き込むのは `syncFavorite()` ですが、この関数は

```js
if(!result || !(await Favorites.has(favoriteCode, src))) return;   // ← 登録前は即return
```

と、**すでに登録済みの銘柄しか更新しません**。
そのため「スキャン → ☆で登録」の順に操作すると、
スキャン時点では未登録なので `syncFavorite` が素通りし、
登録時にはスコアを書き込む処理がない、という空白が生まれていました。

**② お気に入り側でスキャンしても、スコア分析ページに共有されなかった**

`scanOneFav()` は `Favorites.updateScore()` は呼ぶものの、
`dataCache`（スコア分析ページが参照するメモリキャッシュ）には入れていませんでした。
そのため、お気に入りでスキャンした直後にスコア分析ページを開くと、
同じ銘柄を取り直していました。

### 修正

- **☆で登録した瞬間に、直前の解析結果を書き込む**ようにしました
- **`scanOneFav()` の結果を `dataCache` にも格納**し、双方向で共有します
- キーの生成を `cacheKeyFor()` に統一しました（自前で組み立てるとズレて共有されないため）
- 結果→保存項目の変換を `favPayloadFromResult()` に共通化し、
  どちらの経路から更新しても同じ項目が入るようにしました
- お気に入りページを開いたままスキャンした場合も即座に再描画します

---

## B. 需給データのスコア組み込み

v10.8・v10.9 で集めた `data/supply.json` を読み、
**エントリー準備度への補正**として使います。Layer 1〜5 のスコア本体は変更していません。

### B-1. 配分を日米で変えている理由

需給バックテスト v2.1 で、次のことが分かっています。

| 市場 | 総合スコアとの関係 | 意味 |
|---|---|---|
| 日本 | スコアが低いほど信用買残Zが高い（total≦20で **+1.42σ**、40-60で+0.23σ） | 需給情報がすでにスコアへ織り込まれている |
| 米国 | 空売り出来高Zはどのスコア帯でもほぼ無変化（-0.18〜+0.10） | スコアと独立した情報 |

つまり**日本株で需給を厚く配分すると、同じ情報を二重に数える**ことになります。
そこで補正の上限を **米国 ±6pt / 日本 ±3pt** としました。

### B-2. 採用した指標と実測値

| 指標 | 米国 | 日本 | 出典 |
|---|---|---|---|
| 空売り出来高Z ≧2（急増） | +0.56 | — | v6.0 §1 |
| 空売り出来高Z ≦-2（急減） | -0.44 | — | v6.0 §1 |
| 信用買残Z ≧2 | — | +1.36 | v7.0 §8-1 |
| 空売り比率 ≧45% | — | +0.73 | v7.0 §8-1 |
| AAII spread ≦-30 | +1.25 | +1.25 | v7.0 §6-1 |

**買残混雑スコアは採用していません。** 単独成績が -0.05・陽性率48%で
無情報だったためです（v7.0）。ゲートにも使いません。

### B-3. 鮮度の扱い

信用残は約4日、FINRAは1日遅れて公表されます。
`maxAgeDays`（米5日 / 日12日）を超えたデータは**寄与を半分に割り引き**、
画面に「鮮度低」のバッジを出します。

### B-4. データが無くても動きます

`data/supply.json` が存在しない（バッチ未実行）場合は静かに無効化され、
従来どおりのスコアになります。コンソールに一行だけ記録が出ます。

---

## C. スコア推移チャートの拡張

### C-1. 追加した系列

| # | 系列 | 色 | 既定 |
|---|---|---|---|
| 1 | テクニカル合成（従来の線） | 紫 | 表示 |
| 2 | **総合判定スコア** | 青 | 表示 |
| 3 | **エントリー準備度** | 緑 | 表示 |
| 4 | **コンフィデンス** | 茶 | 非表示 |
| 5 | **20日先の上昇確率** | 桃（破線） | 非表示 |
| — | **深度D** | 橙の棒グラフ | 表示 |
| — | VIX/日経VI（右軸） | 橙の線 | 表示 |

系列が多いと読みづらいため、**凡例をタップして表示/非表示を切り替えられる**ようにしました。
既定では4つ（合成・総合・準備度・深度D）だけを出しています。

**深度Dは折れ線ではなく棒グラフ**です。0を超えた日だけ、
チャート下部の専用帯に描きます。深度は「発生していない日」が大半のため、
折れ線にすると0の横ばい線が延々と続いて読みにくくなるからです。

**20日上昇確率は方向感のある日だけ点灯**します。
基準差±3pt未満は「方向感なし」として数値を出さない設計（v8.0 §10-2 B5）に従い、
チャートでも線が途切れます。

### C-2. 買いサイン／天井サインのアイコン

エントリー準備度の較正済み閾値（v4.0）に合わせて**3段階**に分けました。

| 条件 | 記号 | 色 |
|---|---|---|
| ER ≧ 60（強い準備） | ▲ | 濃い緑 |
| ER ≧ 50（準備あり） | ▲ | 緑 |
| ER ≧ 35（兆候） | △ | 薄い緑 |
| 総合判定 ≧ 80（天井警戒） | ▼ | 赤 |

1年表示では密になりすぎるため、間引いて描画します。

### C-3. 1年スパンの追加

レンジ切替に「1年」（250営業日）を追加しました。
履歴の計算範囲も 132日 → **250日**に拡大しています。

**計算時間は250日で約430ms**（実測）でした。
描画後に `requestIdleCallback` で走るため、初期表示は待たされません。

---

## D. ローソク足チャートに出来高

下部30%に出来高を重ねました。陽線は緑、陰線は赤で色分けしています。
価格とは別のスケールにしているため、ローソク足の縦幅は狭くなりません。

---

## E. お気に入り一覧の情報量

各行の下に指標の帯を追加しました。

```
トヨタ自動車            7203.T · 5分前            3,150   +0.8%   [大底 62.1] [天井 18.3]
総合 41.2   準備度 55 準備完了   20日上昇 62% /平常55.6   深度D 3.2   確度 71
```

| 項目 | 内容 |
|---|---|
| 総合 | 総合判定スコア |
| 準備度 | エントリー準備度 + **状態コメント**（待機／監視／準備完了 など） |
| 20日上昇 | 上昇確率と**平常時の値**（基準差方式のため両方を出します） |
| 深度D | 0を超えたときだけ表示 |
| 確度 | コンフィデンス |

準備度は 60/50/35 の閾値で色が変わります。

---

## F. 変更ファイル

| ファイル | 変更 |
|---|---|
| `index.html` | 4,083行 → 4,490行。SUPPLYモジュール・履歴拡張・チャート改修・お気に入り拡張・同期修正 |
| `CHANGELOG_v11_0.md` | 本ファイル（新規） |

`batch/`・`sw.js`・`.github/` は **v10.9 から変更していません**。

---

## G. 動作確認（34項目すべて通過）

| 検証項目 | 結果 |
|---|---|
| 需給データが無くても従来どおり動く | supply=null で正常動作 |
| supply.json の読み込み | 1銘柄・市場データとも読める |
| 空売り出来高の急増（Z≧2）を検出 | +0.56pt を検出 |
| 補正が上限内に収まる | 米国6pt以内 |
| 日本で米国専用指標を採用しない | evaluate が null |
| 上限が日米で異なる | 米6pt / 日3pt |
| 履歴250点の計算 | overall / er / conf / depth すべて全日算出 |
| サインの3段階判定 | ER 65→tier3、52→tier2、38→tier1、20→なし |
| 天井サイン | 総合85→▼ |
| 3段階で色が異なる | 3色すべて別 |
| SVG生成・複数系列 | polyline 2本以上 |
| 深度Dの棒グラフ | rect が描画される |
| サインアイコンの描画 | ▲ が出力される |
| 凡例トグルの生成 | 深度Dのトグルを含む |
| お気に入り保存項目 | score/er/erLabel/depth/conf/prob すべて含む |

既存テストも回帰なしです（v10.5系28項目・パターン22項目・作業②10項目、すべて通過）。

---

## H. 注意点

- **需給補正は `data/supply.json` がある場合のみ効きます。**
  まだバッチを回していない場合は、
  `py batch\import_supply_history.py --finra ...` で過去分を入れ、
  `py batch\main.py supply` を実行してください。
  Zスコアの計算には最低10営業日ぶんの履歴が必要です。
- **履歴のファンダメンタルは全期間で現在値を使っています。**
  過去のファンダを保持していないためで、総合判定スコアのファンダ寄与は
  日ごとに変化しません。テクニカル合成スコア（従来の線）は影響を受けません。
- 上昇確率の線は途切れます。これは不具合ではなく、
  基準差±3pt未満を「方向感なし」として出さない設計によるものです。
- 1年表示はサインアイコンを間引いて描画します。
