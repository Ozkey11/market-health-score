# Market Health Score — 変更点 (v11.4 / 2026-07-27)

**Put/Call Ratio の系列不一致の修正／偏差値と生値の併記**

v11.3 からの差分です。`index.html` と `batch/fetch_fundamentals.py` の2ファイルです。

---

## A. Put/Call Ratio — 系列が食い違っていました

### A-1. ご指摘のとおり、値がおかしい状態でした

他サイトと突き合わせた結果です（2026年7月中旬〜下旬）。

| 系列 | 実際の水準 | 出典 |
|---|---|---|
| **エクイティ** P/C | 0.57〜0.62 | MacroMicro 1640（7/14=0.62）、ycharts（7/23=0.61）、thetrading.tools（7/9=0.57） |
| **トータル** P/C | 0.86 | thetrading.tools（7/9） |
| **インデックス** P/C | 1.01 | 同上 |
| アプリの表示 | **0.79** | — |

アプリの0.79はどの系列とも一致していませんでした。

### A-2. 根本原因は「較正した系列と取得する系列が違う」こと

バックテストで使ったのは TradingView の **USI:PCC = トータル**でした。
プロジェクト内のCSVを確認すると、平均0.945・中央値0.921・範囲0.56〜3.15です。
スコアの閾値（静けさの天井 `pcr:0.8` など）もこの分布で較正しています。

ところが `batch/fetch_fundamentals.py` は

- CBOEページから `EQUITY PUT/CALL RATIO` を検索
- MacroMicro **series/1640**（= エクイティ）

と、**エクイティ系列**を取りにいっていました。
エクイティはトータルより構造的に0.3ほど低いため、
**常に「強気（complacency）寄り」に判定される**状態でした。

### A-3. もうひとつの問題 — 正規表現が緩すぎた

```python
m = re.search(r'(?:Latest|latest|最新)[^0-9]*?(0\.\d{2,3})', html)
```

「latest」という語の近くにある `0.XX` を拾うだけなので、
ページ内の無関係な数値を取ってしまう危険がありました。
0.79という中途半端な値は、これが原因である可能性が高いと考えています。

### A-4. 修正

1. **トータルを第一候補**にしました（バックテストと系列を揃える）
2. CBOE側は `TOTAL PUT/CALL RATIO` → `EQUITY...` の順で厳密に検索
3. MacroMicro側は **series/1651（トータル）→ 1640（エクイティ）** の順
4. 正規表現を「日付の直後の数値」に限定（`2026-07-14 · 0.62` の形）
5. 取得した**系列名と日付を記録**し、アプリで判別できるようにしました

エクイティしか取れなかった場合も換算はしません。系列が違えば分布も違うためです。
代わりに画面に **「※エクイティ系列」** と橙色で表示し、
閾値と系列が食い違っていることが分かるようにしました。取得日も併記します。

---

## B. MOVE指数 — 数日古い値でした

| 出典 | 値 |
|---|---|
| TradingView（TVC:MOVE） | 66.6（週間 -10.1%、月間 -14.5%） |
| アプリ表示 | 70.9 |

系列そのものは正しく、**数日分の遅れ**によるものでした。
MOVEは足元で急低下しているため、数日の差が6%の乖離になっています。

こちらは取得ロジックの誤りではないため、
A-4 で入れた「取得日の併記」で鮮度が分かるようにする対応にとどめました。

---

## C. 偏差値だけでなく生値も表示するようにしました

ご指摘のとおり、銅先物・原油WTI・米ドル指数は**1年偏差値（σ）のみ**の表示でした。
実際の価格が分からないため、生値を併記します。

```
銅先物(1年偏差値)      +1.25σ (6.32ドル/lb)
原油WTI(1年偏差値)     +1.04σ (89.3ドル)
米ドル指数(1年偏差値)   +2.30σ (101.43)
```

**スコアの計算は従来どおり偏差値で行います**（生値は表示のみ）。
偏差値を使っているのは、銅の6.32ドルや原油の89ドルという絶対値には
「高い/安い」の基準がなく、その銘柄自身の1年間の分布と比べる方が
意味を持つためです。生値はあくまで「実際にいくらなのか」を確認するためのものです。

なお **MOVE指数は元々が生値表示**（70.9）で、偏差値ではありません。
閾値も「120超で不安、170超はパニック」と絶対値で設定されています。

---

## D. 変更ファイル

| ファイル | 変更 |
|---|---|
| `batch/fetch_fundamentals.py` | `_fetch_pcr()` を全面書き換え（系列統一・正規表現の厳格化・系列名の記録） |
| `index.html` | 生値の併記、PCR系列と取得日の表示 |
| `CHANGELOG_v11_4.md` | 本ファイル（新規） |

---

## E. 動作確認

| 検証項目 | 結果 |
|---|---|
| CBOEからトータルを抽出 | `TOTAL PUT/CALL RATIO ... 0.86` → 0.86 |
| MacroMicroから日付つきで抽出 | `2026-07-14 · 0.62` → (2026-07-14, 0.62) |
| 系列名がアプリまで引き継がれる | total / equity とも判別可 |
| 取得日が引き継がれる | 2026-07-24 |
| 銅に生値が併記される | `+1.25σ (6.32ドル/lb)` |
| 原油に生値が併記される | `+1.04σ (89.3ドル)` |
| ドル指数に生値が併記される | `+2.30σ (101.43)` |
| 偏差値も残っている | 正常 |

既存テストも回帰なしです（28 / 22 / 34 / 21 / 23 / 10 / 25 項目すべて通過）。

---

## F. 注意点と残る課題

- **次回のバッチ実行から、PCRの値が0.2〜0.3ほど高くなります。**
  これは異常ではなく、エクイティ系列からトータル系列へ戻したためです。
  スコアの閾値と系列が揃うので、判定はむしろ正しくなります。
- CBOEもMacroMicroも公式APIではないため、ページ構造の変更で
  再び取得できなくなる可能性があります。失敗時は `data_quality.json` に記録されます。
- MOVEの遅れはYahoo側の更新タイミングによるものです。
  リアルタイム性が必要であれば別の取得元を検討する必要があります。
- 原油・銅・ドル指数の**閾値そのものは未検証**です。
  現在は「1σで加点開始、3σで満点」といった一般的な設定で、
  バックテストによる較正は行っていません。
