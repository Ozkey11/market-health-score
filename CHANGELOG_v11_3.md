# Market Health Score — 変更点 (v11.3 / 2026-07-27)

**相場格言・天井形成・一目均衡表の検証結果を反映／不具合3件の修正**

v11.2 からの差分です。`index.html` のみの変更で、`batch/` 配下はそのままです。

---

## A. 不具合の修正

### A-1. Put/Call Ratio がアプリに反映されない

**原因**: `_fundJson` は `fetchFundamentalAuto()` の中でしか設定されませんが、
`run()` は「ファンダのキャッシュが古いときだけ」その関数を呼ぶ設計でした。

```js
const needFund = FundPersist.age() > CONFIG.priceCache.maxAge;
if(needFund) fetches.push(fetchFundamentalAuto(true));   // ← 新しいと呼ばれない
...
if(!sentiment.pcr && _fundJson?.indicators?.pcr){ ... }  // ← _fundJson が null で空振り
```

ページを開き直した直後などキャッシュが新しい場合、`_fundJson` が null のままとなり、
**バッチ側で MacroMicro 経由の取得に成功していても画面に出ない**状態でした。

**修正**: 必要になった時点で読み込む `ensureFundJson()` を新設し、
localStorage にも退避するようにしました。全4か所の注入処理を
`injectFundIndicators()` に集約しています。

これで PCR のほか、シラーPER・イールドカーブなど fundamentals.json 由来の指標が
確実に反映されます。

### A-2. お気に入りリストが長くなる

スコアページのお気に入りバーを**折りたたみ可能**にし、
既定では**先頭12件**だけを表示するようにしました。
「▼ 残り○件を表示」で全件、見出しのタップで折りたたみ。
折りたたみ状態は localStorage に保存されます。

サイドバーではなく折りたたみを選んだのは、既存のレイアウト（縦1カラム）を
崩さずに済み、スマートフォンでも同じ操作になるためです。

### A-3. ザラ場中の株価が反映されない

Yahoo Finance の日足はザラ場中に当日のバーを返さないため、
「今の株価でスコアがどうなるか」を確認できませんでした。

**仮値の入力欄**を新設しました。現在値を入力すると、その株価を当日の終値とみなして
**全スコアを再計算**します（総合判定・準備度・押し目スコア・上昇確率まで含む）。

- 当日のバーが無ければ1本追加し、あれば終値を差し替えます
- 仮値はメモリ上のみで、保存もキャッシュもしません
- 銘柄を変えると自動的に解除されます
- 最新の終値がいつのものかも併記します

なお「ザラ場中は始値を仮表示」という案は採用しませんでした。
始値は現在値と乖離することが多く、
**誤った値で自動計算されるより、入力していないことが分かる方が安全**と判断したためです。

---

## B. 相場格言の検証結果を反映

総合判定の下に「相場格言との照合」パネルを追加しました。
スコアには加算せず、**現在地の説明**として表示します。

### B-1. 採用した3つ

| 格言 | 実装 |
|---|---|
| **半値八掛け二割引** | 高値から-40%以下で発火。**-50〜-60%の帯は「最も報われない帯」と警告**（半値付近で止まったのは17%、最安値の中央値-73.6%） |
| **半値戻しは全値戻し** | 直近の下落に対する戻り率を計算し、1/3・半値・2/3の到達を判定。半値戻し後の全値戻し確率67.9%（無条件49.2%）を表示。60%超の暴落からは成立しない旨も注記 |
| **天井直前は急ピッチ** | 直近20日の上昇角度を年率換算し、天井時（131%）・非天井時（70%）と比較。加速の度合いから**その後60日の下げ幅の目安**（急加速なら-21.25%）を提示 |

### B-2. 採用しなかった4つ（理由つきで画面に残す）

プルダウンで確認できます。「なぜ実装していないのか」が後から分かるようにするためです。

- **半値押し** — 押し幅の中央値は68.1%で、1/3・1/2・2/3に集中しない。35.8%は全戻し
- **大保ち合いは大相場** — 保ち合いの長さと放れ後の値幅の相関は**-0.241**。長いほど値動きは小さい
- **保ち合い放れにつけ** — 上放れ追随は60日+1.76%と弱く、下放れ追随（売り）は不利
- **閑散に売り無し** — 閑散なほど成績が悪く下落リスクも高い

---

## C. 一目均衡表の検証（新規分析）

日米73銘柄・395,553営業日行で検証しました。

### C-1. 雲のねじれは天底を当てない

| 検証項目 | 実測 | 偶然の期待値 | リフト |
|---|---|---|---|
| ねじれ±10日に天井 | 23.8% | 30.3% | **0.79倍** |
| ねじれ±10日に底 | 23.4% | 29.3% | **0.80倍** |

**むしろ基準より低い**という結果でした。ねじれの位置は26日先に分かるため
先行指標として期待されますが、天底との関係は確認できませんでした。
これは「採用しなかった格言」に含めています。

### C-2. 一目の状態は「位置判定」には使える

| 状態 | 20日勝率 | 天井率 | 底率 |
|---|---|---|---|
| 雲の上 | 53.9% | **2.60%**（1.8倍） | 0.09% |
| 雲の中 | 53.4% | 0.87% | 1.13% |
| 雲の下 | **55.3%** | 0.10% | **3.24%**（2.3倍） |
| 三役好転 | 53.8% | **3.14%**（2.2倍） | 0.00% |
| 三役逆転 | **55.7%** | 0.01% | **4.04%**（2.9倍） |

（基準: 勝率54.4% / 天井率1.44% / 底率1.39%）

**方向の予測としては逆神**です（雲の下・三役逆転の方が成績が良い）。
一方で**天井圏か底圏かの位置判定としては有効**で、三役逆転は底の出現率が2.9倍でした。
アプリではこの読み方を明記して表示しています。

### C-3. 組合せ（ねじれ→雲上→MA ゴールデンクロス）

ご質問の連鎖も検証しました。

| 条件 | 件数 | 20日勝率 | 60日勝率 |
|---|---|---|---|
| 好転ねじれ + 雲の上 + MA GC | 1,342 | **57.15%** | 55.74% |
| 三役好転 + MA GC | 1,614 | 53.53% | 54.40% |
| MA GC 単独 | 10,843 | 54.81% | 56.21% |
| 逆転ねじれ + 雲の下 | 37,842 | 55.91% | **58.92%** |
| （全体） | 395,553 | 54.37% | 56.82% |

「好転ねじれ + 雲の上 + MA GC」は20日で+2.8ptと**短期には機能**しますが、
60日では全体を下回ります。一方で**最も成績が良いのは「逆転ねじれ + 雲の下」**（60日+58.92%）で、
ここでも逆神の構造が出ました。

効果が短期に限られ、かつ既存の逆張りスコアと方向が競合するため、
**今回はスコアへの組み込みは見送り**、状態表示にとどめています。

---

## D. 変更ファイル

| ファイル | 変更 |
|---|---|
| `index.html` | 4,898行 → 5,208行。ensureFundJson・PROV・WISDOM・CONFIG.wisdom・お気に入り折りたたみ |
| `CHANGELOG_v11_3.md` | 本ファイル（新規） |

---

## E. 動作確認（23項目すべて通過）

| 検証項目 | 結果 |
|---|---|
| ensureFundJson で fundamentals.json を読む | 正常 |
| PCR が sentiment に注入される | 0.79 を注入 |
| localStorage への退避 | 正常 |
| 既に PCR がある場合は上書きしない | 正常 |
| 仮値で終値・meta が差し替わる | 正常 |
| 元データを書き換えない | 正常 |
| 別銘柄には適用されない | 正常 |
| 仮値でスコアが変わる | 総合 61.1 → 20.3（-15%入力時） |
| 一目の位置判定が出る | 正常 |
| 高値-55% で「最も報われない帯」を警告 | 正常 |
| 不採用の格言5件が登録されている | 正常 |

既存テストも回帰なしです（v10.5系28 / パターン22 / v11.0系34 / 押し目21 / 修正10）。

---

## F. 注意点

- 相場格言の当てはめは**スコアに加算していません**。現在地の説明としてのみ表示します。
  格言はいずれも効果量が小さいか、既存スコアと方向が競合するためです。
- 一目均衡表の組合せは短期（20日）でのみ有効で、60日では消えます。
  スコアへの組み込みは見送りました。
- 仮値は**その場限り**です。リロードや銘柄変更で消えます。
  正式な終値が確定したら通常の取得に戻してください。
- 雲のねじれの検証は「ねじれ発生位置の±10日に天底があるか」で測りました。
  「ねじれを何日前に知って構えるか」という運用面の検証は行っていません。
