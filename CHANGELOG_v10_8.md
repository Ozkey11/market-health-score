# Market Health Score — 変更点 (v10.8 / 2026-07-25)

**作業③: 米国の空売り出来高（FINRA）と日本の信用取引残高（JPX）の取得を追加**

v10.7（パターン追加）からの差分です。今回は `batch/` 配下のみの変更で、
`index.html` と `sw.js` は v10.7 のままです。

---

## ⚠ 最重要 — JPXは2026年9月28日に公表様式を変更します

作業中に、JPXが**信用取引残高の集計システムをリプレースする**ことが分かりました
（2026年7月6日付でJPXが公表）。

| | 現行（〜2026/9/25申込分） | 新（2026/9/28〜） |
|---|---|---|
| 帳票名 | 銘柄別信用取引**週末**残高 | 銘柄別信用取引残高 |
| 公表頻度 | **週次**（火曜16:30） | **毎営業日**（16:00） |
| ファイル形式 | PDF | PDF（Excelの提供はなし） |
| 1銘柄の行数 | 1行 | **2行**（株数／金額） |
| 追加項目 | — | 金額、上場株式数比 |
| 並び順 | 銘柄種別・市場区分ごと | **銘柄コード順** |

**良い知らせ**: 全銘柄の信用残が週次から**日次**になります。アプリにとっては大きな改善です。
**悪い知らせ**: PDFのままで、様式が変わるため単純なスクレイパーは9月に壊れます。

そのため本実装は**様式を自動判定して解析器を切り替える構造**にしました。
新様式の解析器は、JPXが公表した様式見本（別紙）をもとに書いてあり、
テストも通していますが、**実物での検証は9月以降でないとできません**。
切替後は必ず `data/data_quality.json` を確認してください。

なお9/27のシステム移行が失敗した場合は延期されます（JPXが当日20時頃に告知）。

---

## A. 取得先の検討結果

### A-1. ご提案の2ページについて

| ページ | 判定 | 理由 |
|---|---|---|
| `margin/index.html`（個別銘柄信用取引残高表） | **不適** | 対象が**規制措置銘柄・日々公表銘柄・特別注意銘柄などに限られ**、全銘柄ではありません。実際、確認時点で掲載は2026年5月28日分の1件のみでした |
| `margin/05.html`（銘柄別信用取引週末残高） | **適** | 全銘柄（4,253銘柄）が対象。これを採用しました |

ご提案どおり **05.html が正解**でした。

### A-2. J-Quants API（JPX公式）は有料でした

無料で継続運用したいとのことでしたので念のため確認しましたが、
信用取引週末残高（`/markets/weekly_margin_interest`）は
**スタンダードプラン（月額3,300円）以上**が必要で、無料プランでは提供されません。
無料プランは12週間遅延でもあります。

将来的にPDF解析の保守が負担になるようでしたら、月3,300円で解析不要になる
選択肢として記憶しておくとよいと思います。

### A-3. 個人サイトからの脱却について

ご認識のとおりです。JPXは公式かつ無料で、全銘柄・長期の実績があります。
唯一の難点がPDFのみという点で、これは今回の実装で解決しています。

---

## B. 追加したファイル

### B-1. `batch/fetch_supply_us.py` — FINRA日次空売り出来高

- 取得元: `https://cdn.finra.org/equity/regsho/daily/CNMSshvol{YYYYMMDD}.txt`
- APIキー不要・無料・毎営業日更新
- 直近12営業日ぶんの**未取得分だけ**を取りにいく差分方式
- 休場日（404）はエラーとして扱わない
- ウォッチリストの銘柄＋指数ETF（SPY/QQQ/IWM/DIA）に絞って保存

いただいた `finra_daily_short_volume_downloader.py` のURL形式・検証ロジックを
そのまま踏襲しています。過去分の一括取得は引き続きそちらをお使いください。

### B-2. `batch/fetch_supply_jp.py` — JPX信用取引残高

- 取得元: `margin/05.html` に掲載されたPDF
- **URLを日付から組み立てず、一覧ページのリンクを拾う方式**にしました。
  ディレクトリ名（`tvdivq0000001rnl-att`）は将来変わりうるためです
- 様式を自動判定（現行 / 2026年9月〜）して解析器を切り替え
- PDFに書かれた「総合計 ○○銘柄」と解析件数を突き合わせ、
  9割を下回ったら警告を残す**自己検証**つき

### B-3. `batch/import_supply_history.py` — 過去分の一括取り込み

Zスコアやパーセンタイルの計算には履歴が必要なので、
手元のファイルをまとめて流し込む道具を追加しました。

```
py batch\import_supply_history.py --finra path\to\finra_daily_short_volume
py batch\import_supply_history.py --jpx   path\to\jpx_pdf
```

既定ではウォッチリストの銘柄に絞ります（`--all` で全銘柄）。

---

## C. 変更したファイル

### C-1. `batch/db.py` — 銘柄別の需給テーブルを追加

既存の `supply_demand_daily` は主キーが `(market, date, metric_name)` で
**銘柄の軸がなく**、市場全体の指標用でした。銘柄別データ用に新テーブルを追加します。

```sql
CREATE TABLE supply_symbol_daily (
    symbol, date, metric_name, value,
    release_date,          -- 公表日（対象日と分ける）
    source, updated_at,
    PRIMARY KEY (symbol, date, metric_name)
);
```

`release_date` を持たせたのは**PIT整合性**のためです。信用残は約4日、
FINRAは1日遅れて公表されるため、対象日だけで参照すると未来の情報を使ってしまいます。

### C-2. `batch/main.py` — Step 5（需給）を追加

```
Step 1 価格 → Step 2 マクロ → Step 3 センチメント
→ Step 4 ファンダ → Step 5 需給 → Step 6 配信JSON
```

`py batch\main.py supply` で需給だけ実行することもできます。

### C-3. `batch/build_json.py` — `data/supply.json` の生成

銘柄をキーに、指標ごとの時系列（対象日・値・公表日）を配信します。

### C-4. `requirements.txt` — PDF解析ライブラリ

85ページのPDFで実測した結果、**pdfplumberは29.6秒と遅すぎるため不採用**にしました。

| ライブラリ | 所要時間 | 判定 |
|---|---|---|
| pymupdf | 0.23秒 | 任意（あれば使う） |
| pdftotext | 0.25秒 | 外部バイナリのため不採用 |
| **pypdf** | **3.27秒** | **既定** |
| pdfplumber | 29.63秒 | 不採用 |

pymupdfがあれば使い、無ければpypdfに自動で切り替わります。

---

## D. 見つけた不具合

### FINRAの優先株シンボルを壊していました

実装当初、シンボルを大文字化していましたが、FINRAの優先株シンボルは
**小文字を含みます**（例: `AHTpF` = Ashford Hospitality 優先株F）。
大文字化すると `AHTPF` となり、別銘柄と区別できなくなります。

既存の正規化済みCSVと突き合わせて発見しました。原文のまま保持し、
ウォッチリストとの突合だけ大文字で行うよう修正しています。

---

## E. 動作確認

サンドボックスは外部ネットワークが遮断されているため、
**解析ロジックは実データで**、**通信部分は失敗時の挙動で**検証しました。

### E-1. FINRAパーサ（実データ）

| 検証項目 | 結果 |
|---|---|
| 同一銘柄が市場別に複数行あるケースの合算 | 正しく合算（500000+365474=865474） |
| 比率の計算 | SPY 0.412051 |
| 出来高0の銘柄でゼロ除算しない | None を返す |
| **既存の正規化済みCSV 200銘柄との突合** | **不一致0件** |

### E-2. JPXパーサ（実PDFテキスト）

プロジェクト内の実データ（2026年7月3日申込分）で検証しました。

| 検証項目 | 結果 |
|---|---|
| 様式の自動判定 | current と判定 |
| 申込日の抽出 | 2026-07-03 |
| **抽出件数** | **4,240 / 4,253銘柄（99.7%）** |
| トヨタ(7203) 売残2,171,500 / 買残22,198,100 | 一致 |
| コード変換 16050 → 1605.T | 一致 |
| 英字入りコード 166A0 → 166A.T | 一致 |
| 社名とコードが空白なしで繋がる行 | 拾える |
| 外国籍ETF（US/SG/JE始まりのISIN） | 拾える |
| 前週比の「▲」を負数として解釈 | 一致 |

列の並びは**算術的に確認**しました。
「一般信用売 + 制度信用売 = 売残高」が成立することを全行でチェックしています
（例: タマホーム 523,900 + 700,700 = 1,224,600）。

### E-3. 新様式（2026年9月〜）のパーサ

JPXの様式見本をもとに検証しました。

| 検証項目 | 結果 |
|---|---|
| 新様式と判定 | 2026 と判定 |
| 株数行のみ拾い、金額行を無視 | 一致 |
| 値の解釈 | 売9,300 / 買177,900 |
| 「▲」の解釈 | -1,100 |

### E-4. 統合

| 検証項目 | 結果 |
|---|---|
| 全6ステップの実行 | 完走（終了コード0） |
| 通信断でもクラッシュしない | 失敗を記録し後続を継続 |
| 失敗の記録に取得先URLが付く | 13件すべてに付与 |
| 一括インポータ | FINRA 1日分・JPX 4,175銘柄を取り込み |

テスト合計 **25項目すべて通過**しています。

---

## F. 注意点

- **JPXのPDF解析は、実際のPDFファイルでの検証ができていません。**
  サンドボックスからJPXへ接続できないためです。検証に使ったのは
  プロジェクト内にあるテキスト（実物のPDFから抽出されたもの）です。
  初回実行時は `data/data_quality.json` の解析件数を必ず確認してください。
  「総合計○○銘柄」との突合を自動で行い、9割を下回れば警告が出ます。
- **9月28日の様式変更後は、新様式パーサの実地検証が必要です。**
- 信用残は公表が約4日遅れます。アプリ側で参照する際は `release` を見て、
  その時点で見られた情報かを判定してください（`supply.json` に含めています）。
- 日本株は**スコアが低いほど信用買残Zが高くなる**（total≦20で+1.42σ）ため、
  需給を独立レイヤーとして重く配分すると二重計上になります（v2.1 の指摘）。
  スコアへの組み込みは作業④以降で慎重に行う想定です。
- FINRAの過去分（2018年8月〜）は容量が大きいため、既定ではウォッチリストに
  絞って取り込みます。全銘柄が必要なら `--all` を付けてください。

---

## G. 変更ファイル

| ファイル | 変更 |
|---|---|
| `batch/fetch_supply_us.py` | 新規（FINRA日次空売り出来高） |
| `batch/fetch_supply_jp.py` | 新規（JPX信用取引残高・様式自動判定） |
| `batch/import_supply_history.py` | 新規（過去分の一括取り込み） |
| `batch/db.py` | `supply_symbol_daily` テーブルと upsert 関数を追加 |
| `batch/main.py` | Step 5（需給）を追加、Step構成を6段に |
| `batch/build_json.py` | `build_supply()` を追加 |
| `batch/sources.py` | FINRA・JPXの取得先リンクを登録 |
| `requirements.txt` | pypdf（必須）・pymupdf（任意）を追加 |
| `CHANGELOG_v10_8.md` | 本ファイル（新規） |

`index.html`・`sw.js`・`.github/` は **v10.7 から変更していません**。

---

## H. 次のステップ

- **作業④** 日経空売り比率・AAIIセンチメントの追加
- そのあと、取得した需給データをスコアへ組み込む改修（二重計上に注意）

---

## I. 導入方法

`batch/` フォルダと `requirements.txt` を置き換えてください。
`index.html` と `sw.js` は v10.7 のままで構いません。

初回は過去分の取り込みをおすすめします。

```
py -m pip install -r requirements.txt
py batch\import_supply_history.py --finra path\to\finra_daily_short_volume
py batch\main.py supply
```
