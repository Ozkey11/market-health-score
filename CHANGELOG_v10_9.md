# Market Health Score — 変更点 (v10.9 / 2026-07-25)

**作業④: 日経空売り比率（JPX）と AAII センチメントの追加**

v10.8（FINRA・JPX信用残）からの差分です。今回も `batch/` 配下のみの変更で、
`index.html` と `sw.js` は v10.7 のままです。

これで当初の作業①〜④がすべて完了しました。

---

## A. 追加した2つの指標

### A-1. 日経空売り比率（JPX 空売り集計・日次）

- 取得元: `https://www.jpx.co.jp/markets/statistics-equities/short-selling/index.html`
- APIキー不要・無料・**毎営業日**
- 1ページのみの小さなPDF（`{YYMMDD}-m.pdf`）。信用残PDFの27MBとは違い軽量です

**空売り比率の定義について。** JPXのPDFは「実注文(a)」「空売り-価格規制あり(b)」
「空売り-価格規制なし(c)」「合計(d)」に分かれています。
一般に報道される空売り比率は **(b+c)/d** で、JPXのFAQにも
「新聞等の報道ではこの両者を合計して空売り比率と呼んでいる」と明記されています。
本実装もこれに従い、分解値も別途保存しています。

```
2026年5月29日 11,385,187 65.5% 5,175,095 29.8% 814,270 4.7% 17,374,552
                                    ↑29.8%      ↑4.7%
                        空売り比率 = 29.8 + 4.7 = 34.5%
```

分析レポート v7.0 §8-1 では **空売り比率 ≧45% で20日超過 +0.73・陽性率83%** でした。
日本で最も実用的な日次の需給フロー指標です。

### A-2. AAII センチメント（週次）

- 取得元: `https://www.aaii.com/sentimentsurvey`
- 毎週木曜に公表（強気／中立／弱気の%）

分析レポート v7.0 §6-1 では **spread ≦-30 で +1.25**。
米国個人投資家の総悲観が**日本株にも効く**のが確認されています。

---

## B. AAIIは「最新週の自動取得＋過去分の手動取り込み」の二本立てです

AAIIの**全期間スプレッドシートは会員限定**で、自動ダウンロードできません。
そこで次の構成にしました。

| | 手段 | 備考 |
|---|---|---|
| 最新週 | 公開ページから自動取得 | 体裁変更に弱いが、失敗しても記録が残る |
| 過去分 | 手元のxlsを取り込み | すでにお持ちのファイルが使えます |

```
py batch\import_supply_history.py --aaii path\to\aaii_sentiment.xls
```

お持ちの `AAII投資家sentiment_全データ.xls` で実際に検証し、
**1987年7月〜2026年7月の2,030週分**を取り込めることを確認しました。
値が小数（0.36）でも百分率（36.0）でも自動判別します。

---

## C. 見つけた不具合 — 変化幅を水準と取り違えていました

AAIIの公開ページはこう書かれています。

> Bullish sentiment ... **decreased 15.3 percentage points to 29.6%**

当初の実装は「bullish の近くにある最初の百分率」を拾う作りだったため、
**水準の29.6%ではなく変化幅の15.3%を取ってしまう**状態でした。
数値としてはもっともらしく見えるため、気づきにくい種類の誤りです。

対策として、解析前に「N.N percentage points」を先に除去し、
そのうえで「to 29.6%」「is 31.5%」の形を優先して拾うようにしました。
文章形式・表形式・変化幅つき文章の3パターンで検証済みです。

---

## D. 変更ファイル

| ファイル | 変更 |
|---|---|
| `batch/fetch_supply_extra.py` | 新規（空売り比率・AAII） |
| `batch/import_supply_history.py` | `--aaii` を追加 |
| `batch/main.py` | Step 5 に空売り比率・AAIIを追加 |
| `batch/build_json.py` | `supply.json` に市場全体の需給（`market`）を追加 |
| `batch/sources.py` | 取得先リンクを登録 |
| `requirements.txt` | xlrd をコメントで案内（日々のバッチには不要） |
| `CHANGELOG_v10_9.md` | 本ファイル（新規） |

---

## E. 動作確認（22項目すべて通過）

### E-1. JPX 空売り集計パーサ（実PDFのテキスト）

| 検証項目 | 結果 |
|---|---|
| 日付の抽出 | 2026-05-29 |
| 空売り比率 = 29.8 + 4.7 | 34.5% |
| 実注文・合計・空売り代金の抽出 | 一致 |
| 解析できない文字列 | None を返す |

### E-2. 一覧ページからのリンク抽出

| 検証項目 | 結果 |
|---|---|
| `-m.pdf` のみ抽出し業種別（`-g`）を除外 | 一致 |
| 新しい順に並ぶ | 一致 |
| 相対URLを絶対URLへ変換 | 一致 |

### E-3. AAIIパーサ

| 入力の形 | 結果 |
|---|---|
| 文章形式（decreased ... to 29.6%） | 29.6 / 38.9 を取得 |
| 表形式（`<td>Bullish</td><td>37.9%</td>`） | 37.9 / 30.6 を取得 |
| 変化幅つき（rose 5.7 points to 37.9%） | **37.9 を取得**（5.7 ではない） |
| 合計が100%から外れる | 拾い間違いとみなし None |

### E-4. AAII履歴の取り込み（実ファイル）

| 検証項目 | 結果 |
|---|---|
| 取り込み週数 | **2,030週**（1987-07-24 〜 2026-07-02） |
| 小数→百分率の変換 | 0.36 → 36.0 |
| spread の計算 | 36.0 − 14.0 = 22.0 |

### E-5. 統合

Step 5 で「FINRA → JPX信用残 → 空売り比率 → AAII」の4つが順に実行され、
通信断でも後続を止めずに記録することを確認しました。
`supply.json` に市場全体の需給（8,121行）が載ることも確認しています。

---

## F. 注意点

- **JPX・AAIIとも、実サイトへの接続はサンドボックスから遮断されているため未検証です。**
  解析ロジックは実データ（実PDFのテキスト、実xls）で検証していますが、
  通信部分の実地確認は初回実行時にお願いします。
- AAIIの公開ページは体裁が変わりやすく、**自動取得は best-effort** です。
  失敗しても `data_quality.json` に取得先URLつきで記録されるので、
  手入力または `--aaii` での取り込みで補完できます。
- 空売り比率の一覧ページは月ごとのバックナンバー方式です。
  確認時点では直近1か月ぶんのリンクしか掲載されていませんでした。
  長期の履歴が必要なら、バックナンバーページを個別に開いて保存する必要があります。
- 需給指標をスコアへ組み込む際は、**日本株の二重計上**にご注意ください。
  スコアが低いほど信用買残Zが高くなる（total≦20で+1.42σ）ため、
  独立レイヤーとして重く配分すると同じ情報を二重に数えることになります（v2.1）。

---

## G. 作業①〜④の総括

| 作業 | 内容 | 版 |
|---|---|---|
| ① | main.py の全ステップ配線・Actions のコミット対象拡大 | v10.6 |
| ② | ローソク足・チャートパターンを Layer3 へ追加（API不要） | v10.7 |
| ③ | 米SVR（FINRA）+ 日本信用残（JPX）の取得 | v10.8 |
| ④ | 日経空売り比率・AAII の取得 | v10.9 |

**データ取得状況の棚卸し（`data_source_audit_v1_0`）で「取得できていない」と
整理した需給指標は、これですべて取得経路が用意できました。**

作業の過程で見つかった不具合は4件です。

1. `log_quality()` のコミット漏れで失敗ログが13件中4件しか残らない（v10.6）
2. 固定%のしきい値がボラティリティで壊れる／株式分割で標準偏差が破綻（v10.7）
3. FINRAの優先株シンボルを大文字化して別銘柄と混同（v10.8）
4. AAIIの変化幅を水準と取り違え（v10.9）

---

## H. 残っている課題

- **JPXは2026年9月28日に信用残の公表様式を変更します**（v10.8 で詳述）。
  全銘柄の信用残が週次→日次になる一方、様式が変わるため、
  切替後は `data/data_quality.json` の確認が必要です。
- **取得した需給データをスコアへ組み込む改修**は未着手です。
  データは貯まりますが、まだスコアには反映されていません。
  Zスコアやパーセンタイルの計算には履歴が要るので、
  まず `import_supply_history.py` で過去分を取り込んでから着手するのが順序として自然です。
- アプリが Yahoo の**調整前**株価を使っている点（v10.7 で言及）。
  移動平均やZスコアにも影響し得ます。

---

## I. 導入方法

`batch/` フォルダと `requirements.txt` を置き換えてください。
`index.html`・`sw.js` は v10.7 のままで構いません。

初回は過去分の取り込みをおすすめします。

```
py -m pip install -r requirements.txt
py -m pip install xlrd                     # AAIIのxlsを読む場合のみ
py batch\import_supply_history.py --finra path\to\finra_daily_short_volume
py batch\import_supply_history.py --aaii  path\to\aaii_sentiment.xls
py batch\main.py supply
```
