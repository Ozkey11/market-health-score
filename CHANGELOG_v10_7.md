# Market Health Score — 変更点 (v10.7 / 2026-07-25)

**作業②: ローソク足・チャートパターンを Layer3 へ追加（外部API不要）**

v10.6（バッチ配線）からの差分です。今回は `index.html` のみの変更で、
`batch/` 配下と Actions ワークフローは v10.6 のままです。

---

## 最も重要な発見 — 「一律の逆神」ではなかった

分析レポート v8.0 では「教科書の弱気パターンが買いになる（逆神）」と報告しましたが、
実装にあたって全パターンを並べ直したところ、**より正確な規則性**が見えました。

| パターン | 教科書 | 米国実測 | 判定 |
|---|---|---|---|
| 陰の丸坊主 | 弱気 | +0.71 | 買い → **逆神** |
| 陽の丸坊主 | 強気 | −0.83 | 売り → **逆神** |
| 三羽烏 | 弱気 | +0.39 | 買い → **逆神** |
| 赤三兵 | 強気 | −0.56 | 売り → **逆神** |
| 首吊り線 | 弱気 | −0.48 | 売り → 教科書どおり |
| 逆ハンマー | 強気 | +0.57 | 買い → 教科書どおり |
| 強気のはらみ線 | 強気 | +0.53 | 買い → 教科書どおり |
| 弱気のはらみ線 | 弱気 | −0.35 | 売り → 教科書どおり |
| 毛抜き天井 | 弱気 | −0.30 | 売り → 教科書どおり |
| 毛抜き底 | 強気 | +0.17 | 買い → 教科書どおり |

**実体が大きい勢い系（丸坊主・三兵・三羽烏・フラッグ）だけが逆神で、
実体が小さい反転系（はらみ・毛抜き・ハンマー・首吊り）は教科書どおり**でした。

これは v8.0 第8章で書いた「方向ではなく“行き過ぎの大きさ”が情報」という解釈と一致します。
したがって**一律反転はせず、パターンごとに実測の符号をそのまま使う**実装にしました。

---

## A. 追加した内容

### A-1. CONFIG.patterns — 実測値テーブル

採用 25 種（ローソク足 14 種・チャートパターン 11 種）。
各パターンについて日米それぞれの「20日先の超過リターン（%pt）」を保持します。

- 正の値 = その後上がった = 買い方向（Layer3 を底寄りに動かす）
- 負の値 = その後下がった = 売り方向（Layer3 を天井寄りに動かす）
- 0 = そのマーケットでは方向性なしと判定され不採用

**不採用の基準**（機械的に適用）
1. 超過リターンの絶対値が 0.12pt 未満
2. 陽性率が 0.45〜0.55 の無方向帯にある
3. 平均リターンと陽性率の符号が矛盾している

この基準により、ハンマー（US +0.00 / JP +0.00）などは自動的に不採用となりました。

**日米で符号が異なるもの**も、そのまま別々に保持しています。

| パターン | 米国 | 日本 |
|---|---|---|
| ダブルトップ | +0.48（買い） | 方向なし |
| トンボ十字 | 方向なし | −0.58（売り） |
| 塔婆十字 | +0.53（買い） | −0.16（売り） |
| 上昇フラッグ | 方向なし | −0.82（売り） |

### A-2. PAT モジュール — 検出器

外部APIを一切使わず、OHLC だけで計算します。

- **ローソク足**: 丸坊主・十字線系・ハンマー型・はらみ線・毛抜き・寄付坊主・三兵/三羽烏
- **チャートパターン**: フラッグ／ペナント・三角持ち合い3種・ダブルトップ/ボトム・V字

### A-3. Layer3 への組み込み

既存のプラグイン機構（`CONFIRMATION_PLUGINS`）にそのまま載せました。

重みの再配分（合計は 1.00 のまま）:

```
旧: macdDiv 0.30 / rsiDiv 0.30 / mfiDiv 0.20 / obvDiv 0.20
新: macdDiv 0.24 / rsiDiv 0.24 / mfiDiv 0.16 / obvDiv 0.16
    + candlePat 0.12 / chartPat 0.08
```

既存4種の相対比（30:30:20:20）は維持しています。
チャートパターンをローソク足より軽くしたのは、検出定義の揺れが大きいためです。

### A-4. strength（強さ）への対応

`computeLayer3` を拡張し、プラグインが `strength`（0〜1）を返せるようにしました。
検出したパターンの超過リターンの合計に応じて按分されます。
既存のダイバージェンス系は `strength` を返さないため 1 とみなされ、**挙動は変わりません**。

---

## B. 設計上の判断 — パターンが無い日は分母から外す

パターンは「毎日値が出る指標」ではなく「イベント検出器」です。
何も検出されなかった日にも重みの分母へ含めると、**パターンが無い日にも
ダイバージェンス系のシグナルが2割薄まってしまう**という問題がありました。

そこで検出ゼロの日は `applicable:false` を返し、重みの分母から除外しています。

この結果、**パターンが出ていない日のスコアは従来と完全に一致**します。

| 銘柄 | 方向シグナルが出た日 | スコアが変わらなかった日 |
|---|---|---|
| SPY | 43.8% | 56.2% |
| 7203 | 20.6% | 79.7% |

---

## C. 検出頻度の較正 — ここに最も手間をかけました

### 問題

最初の実装では、バックテスト側の検出器と比べて**成立率が最大8.7倍**ありました。

| パターン | 実装（初版） | バックテスト | 倍率 |
|---|---|---|---|
| ダブルトップ | 17.06% | 1.97% | 8.7倍 |
| ダブルボトム | 19.56% | 2.53% | 7.7倍 |
| 上昇三角持ち合い | 12.65% | 1.45% | 8.7倍 |

実測値は「バックテスト側の、より厳しい検出条件で成立した局面」で測ったものです。
検出が緩いままでは、その値をそのまま当てはめられません。

### 対策

日米8銘柄（SPY・NVDA・IWM・WMT・7203・9432・6857・8306／日次ボラ 0.9〜3.0%）で
成立率を実測し、バックテスト側の成立率に合わせて閾値を調整しました。

**最終結果: 全パターンが目標の 0.4〜2.1 倍に収束**

| パターン | 実装US | 目標US | 実装JP | 目標JP |
|---|---|---|---|---|
| 下降フラッグ | 0.77% | 1.69% | 1.40% | 1.85% |
| ダブルトップ | 3.68% | 1.97% | 2.57% | 2.68% |
| V字ボトム | 14.52% | 13.14% | 10.07% | 11.58% |
| 陰の丸坊主 | 0.70% | 1.85% | 3.60% | 1.98% |
| 首吊り線 | 4.71% | 3.78% | 3.82% | 3.38% |

### 較正の過程で見つかった2つの落とし穴

**① 固定%のしきい値は銘柄のボラティリティで壊れる**

V字パターンを「12本で±6%」と固定値で判定したところ、
SPY（低ボラ）では成立率 0.0%、7203 では 1.8倍と極端に偏りました。
銘柄自身のボラティリティを基準にする方式へ変更し、
米 0.8倍 / 日 1.1倍 まで改善しました。

**② 株式分割で標準偏差が壊れる**

ボラティリティを単純な標準偏差で測ると、株式分割の1日で破綻します。

> NTT（9432）の25分割: 4405円 → 171円
> 単純な標準偏差 = **11.53%** ／ 実態 = 0.94%

アプリは Yahoo の調整前 OHLC を使うため、この問題が直撃します。
**MAD（中央絶対偏差）基準の頑健な推定**へ変更し、0.94% と実態に一致するようにしました。

> 補足: この「調整前の株価を使っている」という点は、パターン検出以外の指標
> （移動平均・Zスコア等）にも影響し得ます。今回の作業範囲外ですが、
> 別途ご相談いただければ調整済み終値へ切り替える改修を検討できます。

---

## D. 表示

Layer3 の確認リストに、検出したパターンと**その根拠**を表示します。

```
ローソク足パターン            [パターン(買い寄り)]
  買い寄り 合計+0.24pt ｜ 強気のはらみ線(教科書強気→実測買い +0.24pt)
```

教科書上の方向と実測の方向の両方を出し、食い違う場合は「逆神」と明示します。
テキストレポートにも同じ内訳が出ます。

---

## E. 変更ファイル

| ファイル | 変更 |
|---|---|
| `index.html` | 3752行 → 4074行。CONFIG.patterns・PATモジュール・プラグイン2種・Layer3拡張・表示 |
| `CHANGELOG_v10_7.md` | 本ファイル（新規） |

`batch/` 配下・`sw.js`・ワークフローは **v10.6 から変更していません**。

---

## F. 動作確認

| 検証項目 | 結果 |
|---|---|
| `CONFIG.patterns.enabled=false` で旧版と完全一致 | 4銘柄すべてで最大差 0.000 |
| パターン未検出日のスコア不変 | 335件すべて一致（不一致 0件） |
| 検出器の単体テスト | 22項目すべて通過 |
| 符号の向き（逆神が正しく効くか） | 陰の丸坊主→買い / 陽の丸坊主→売り を確認 |
| 相反パターンの相殺 | 陰陽の丸坊主同時 → 方向なし |
| 日米で符号が異なるパターン | ダブルトップ 米=買い / 日=方向なし を確認 |
| 総合スコアへの影響 | SPY 61.9→61.1（0.8pt）、7203 変化なし |
| 履歴132日分の計算速度 | 421ms → 452ms（1.08倍） |
| v10.5 の既存テスト | 28項目すべて通過 |
| ネットワーク層 | 0.40秒で取得成功（変化なし） |

---

## G. 注意点

- **検出定義はバックテスト側と完全には一致しません。** 一般的な定義に基づく実装であり、
  成立率を 0.4〜2.1 倍の範囲に合わせることで近似しています。
  実測値は「その種類のパターンが出たときの傾向」として使ってください。
- パターンの効果量は ±0.2〜1.4pt と、ダイバージェンス系と同程度です。
  重みを合計 0.20 に抑えているのはこのためです。
- v9.0 で指摘したとおり、**パターンは今回が初評価であり時代別ホールドアウト検証が未了**です。
  不安があれば `CONFIG.patterns.enabled` を `false` にすると、
  検出も加点も止まり従来のスコアへ完全に戻ります。
- 較正は日米8銘柄・各680日で行いました。銘柄数が増えれば最適値は多少動きます。

---

## H. 次のステップ

- **作業③** 米SVR + 日本信用残の取得バッチを追加
- **作業④** 日経空売り比率・AAII を追加

---

## I. 導入方法

置き換えるのは `index.html` の1ファイルだけです。
`batch/` 配下・`sw.js`・`.github/` は v10.6 のままで構いません。
